import { AFFIRMATION_GIVEN_SUBSCRIPTION } from '../subscriptions';

describe('the affirmation subscription', () => {
  it('is defined', () => {
    expect(AFFIRMATION_GIVEN_SUBSCRIPTION).toBeDefined();
  });
});
